server:
  port: 8888

---

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tonytonydileeptony/banking-config-repo
          search-paths:
              - config-server
          clone-on-start: true
